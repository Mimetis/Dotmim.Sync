- Add ScopeName as first paremeter in orchestrators methods
   this is good : await localOrchestrator.GetClientScopeAsync(scopeName, setup); 
   this seems bad:  await agent.SynchronizeAsync(this.Tables, scopeName);

- In creation of sp or table, we have

                if (scopeInfo.Schema == null)
                    return false;

Maybe, we should log something here ?


- SyncContext GetContext(string scopeName) : Add SyncOptions.DefaultScopeName when everything has been refactored


- public LocalOrchestrator(CoreProvider provider, SyncOptions options)  : Add options = default

- agent.Parameters should come directly in SynchronizeAsync() arguments

- ExistScopeInfoTableAsync for localOrchestrator and remoteOrchestrator

- var s = await agent.SynchronizeAsync(SyncType.Reinitialize, setup, SyncOptions.DefaultScopeName, default); make a easiest call method

- test DisableConstraintsAsync()